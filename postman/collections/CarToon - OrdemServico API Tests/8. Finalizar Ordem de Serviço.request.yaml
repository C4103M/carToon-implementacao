$kind: http-request
description: Finaliza a ordem de serviço.
url: "{{baseUrl}}/{{ordemId}}/finalizar"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code é 200 OK', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 8000
