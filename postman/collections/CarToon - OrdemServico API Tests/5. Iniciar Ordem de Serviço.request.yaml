$kind: http-request
description: Inicia a execução da ordem de serviço.
url: "{{baseUrl}}/{{ordemId}}/iniciar"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code é 200 OK', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 5000
